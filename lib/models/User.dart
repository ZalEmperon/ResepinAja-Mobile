class User {
  final int id_user;
  final String username;
  final String deskripsi_user;
  final String no_hp;
  final String role;
  User(this.id_user, this.username, this.deskripsi_user, this.no_hp, this.role);
}
